# PDF-Merge
This Python script merges multiple PDF files into a single document. It scans the current folder, collects all PDF files, and combines them using PyPDF2. The final merged file is saved as one complete PDF, making file management simple and efficient.
